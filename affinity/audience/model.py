#load data from users (improve or update it)
#cluster the data using kmeans (?) > right now only have the csv with the result so load it
#find number of clusters
#return list of target interests

import pandas as pd
clusters = pd.read_csv("affinity/data/data_v2.csv")
